{
  "name": "yourbar-share-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "inspect:staging-secrets": "wrangler secret list --env staging --format pretty",
    "deploy:staging": "npm run validate:staging && wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "check": "npm run validate:config && npm run typecheck && npm test",
    "generate-static-assets": "node tools/generate-static-assets.mjs",
    "prebuild": "npm run generate-static-assets",
    "validate:config": "node tools/validate-community-config.mjs production && node tools/validate-community-config.mjs staging",
    "validate:staging": "node tools/validate-community-config.mjs staging",
    "migrate:staging": "wrangler d1 migrations apply yourbar-community-staging --env staging --remote"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^4.91.0"
  }
}
